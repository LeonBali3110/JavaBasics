public class CubicEquation++ {

	public static void main(String[] args) {
		int x = 3;
		double f = (Math.pow(x, 3)) + 2 * (Math.pow(x, 2)) + (4 * x) + 5;
		System.out.println("f(" + x + ")" + " = " + f);	

        x = 5;
		f = (Math.pow(x, 3)) + 2 * (Math.pow(x, 2)) + (4 * x) + 5;
		System.out.println("f(" + x + ")" + " = " + f);	
		
		x = 10;
		f = (Math.pow(x, 3)) + 2 * (Math.pow(x, 2)) + (4 * x) + 5;
		System.out.println("f(" + x + ")" + " = " + f);	

	}
}